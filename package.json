{
  "name": "discord-bot-dashboard-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "frontend-install": "npm install --prefix discord-bot-dashboard/frontend",
    "frontend-build": "npm run build --prefix discord-bot-dashboard/frontend",
    "install-all": "npm run frontend-install",
    "build": "npm run frontend-install && npm run frontend-build"
  }
}
